describe('Login Integration Test', () => {
    describe('When your pressed submit without ID and/or Password', () => {
        it('should not navigate to the next page and show an error message', () => {});
    });
    describe('When your entered ID and Password', () => {
        it('should not navigate to the next page if user does not exist', () => {});
        it('should navigate to the next page if user exists', () => {});
    });
});
