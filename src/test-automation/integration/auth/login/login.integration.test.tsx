describe('Login Integration Test', () => {
    describe('When you press submit without ID and/or Password', () => {
        it('should not navigate to the next page and show an error message', () => {});
    });
    describe('When you enter ID and Password', () => {
        it('should not navigate and show error if incorrect credential is given', () => {});
        it('should navigate to the next page if correct credential is given', () => {});
    });
});
