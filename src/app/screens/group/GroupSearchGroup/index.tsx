import React from 'react';
import SearchBar from '../../../components/group/SearchBar';

/*
const styles = StyleSheet.create({
    topBar: {
        flexDirection: 'row',
        alignItems: 'center',
        width: '100%',
        paddingTop: 10,
    },
    backButton: {
        zIndex: 1,
    },
    searchBarContainer: {
        flex: 1,
    },
});
*/

function GroupSearchGroup(): JSX.Element {
    return <SearchBar />;
}

export default GroupSearchGroup;
