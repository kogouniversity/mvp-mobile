import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header: {
        marginBottom: 50,
    },
    field: {
        width: '100%',
        margin: 25,
    },
    button: {
        margin: 40,
        width: '100%',
    },
    typo: {
        color: 'white',
    },
});

export default styles;
